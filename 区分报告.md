 区分报告

  我只读取了代码与配置，没有读取 README、方案或其他 Markdown；DevLoop 的结论包含当前工作区未提交的预览/验证实
  现。

  一句话定位：

  - Pisper：本地优先的 Agent 工作台，核心是“长期会话 + 记忆 + 工具 + 多 Agent + 工作流”。
  - DevLoop：面向代码交付的任务控制台，核心是“隔离执行 + Commit 结果 + 审核/冲突处理 + 写入目标分支 + 自动验
    证”。

   维度          Pisper                                          DevLoop
  ━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   核心对象      Agent 会话、目标、计划、工作流                  项目、任务、Revision、Run、审核决定
  ────────────  ──────────────────────────────────────────────  ───────────────────────────────────────────────
   Agent 引擎    内置 Pi Coding Agent，管理模型、工具、会话上    调度本机 Codex CLI / Claude Code CLI
                 下文
  ────────────  ──────────────────────────────────────────────  ───────────────────────────────────────────────
   上下文        有会话树、派生会话、压缩策略、长期记忆、        有任务 Revision、审核反馈、结果 Commit；未见
                 Token 预算                                      完整 Agent 对话上下文持久化
  ────────────  ──────────────────────────────────────────────  ───────────────────────────────────────────────
   并发          一个会话内可派生子 Agent，默认最多 4 个         Worker 可并发领取多个独立任务，配置范围 1-10
  ────────────  ──────────────────────────────────────────────  ───────────────────────────────────────────────
   Git           当前工作区的 diff、commit、push、revert，兼     每个 Run 独立 Worktree、结果 Commit、目标分支
                 容 Git/SVN                                      对齐、冲突预览与人工/Agent 解决
  ────────────  ──────────────────────────────────────────────  ───────────────────────────────────────────────
   审批          工作流节点是否继续执行                          代码结果是否允许写入目标分支，是交付闸门
  ────────────  ──────────────────────────────────────────────  ───────────────────────────────────────────────
   浏览器能力    Agent 可调用浏览器自动化工具执行点击、输入、    服务端从结果 Commit 创建预览，运行
                 截图                                            Playwright、存截图和结果供审核
  ────────────  ──────────────────────────────────────────────  ───────────────────────────────────────────────
   Skill/扩展    项目/全局 Skill、MCP、工具插件、渠道集成        版本化 Skill 快照后整体注入 Runner Prompt
  ────────────  ──────────────────────────────────────────────  ───────────────────────────────────────────────
   工作流        可视化 DAG：条件、并行、审批、通知、定时执行    当前是任务状态机，不是通用 DAG 编排
  ────────────  ──────────────────────────────────────────────  ───────────────────────────────────────────────
   数据层        多数运行时配置使用原子 JSON；记忆使用 SQLite    核心业务状态统一 SQLite + Drizzle 迁移、审计
                 + FTS/本地向量                                  事件和幂等命令
  ────────────  ──────────────────────────────────────────────  ───────────────────────────────────────────────
   桌面形态      Tauri + Rust TUI + Node sidecar                 Electron + Fastify 服务端 + React Web
  ────────────  ──────────────────────────────────────────────  ───────────────────────────────────────────────
   权限边界      会话级执行模式、工作区信任、工具审批            设备角色、任务/审核状态约束、隔离 Worktree 与
                                                                 受控写入

  DevLoop 明显更强的部分

  - 代码交付链完整：结果不会直接污染目标分支，而是经过 Worktree、Commit、Diff、审核、冲突处理和最终写入。
  - 连续审核迭代有明确基线：驳回后的下一轮可以以上一轮结果 Commit 为基础继续执行，并对齐最新目标分支。
  - 预览与验证绑定结果 Commit，而不是让 Agent 自己“声称已验证”；当前代码还能自动识别常见前端启动方式、安装隔离
    依赖、保存 Playwright 截图。

  - 状态可审计且具备幂等控制，适合多端操作和长期任务队列。

  Pisper 明显更强的部分

  - 通用自动化能力更强：工作流支持条件、并行、审批、重试、通知和定时，而不仅是代码任务。
  - 模型层与应用层解耦较好，能管理多 Provider、模型和思考级别。

  关键区别

  Pisper 的“审批”是“是否让工作流继续”，不是 DevLoop 的“是否允许把代码写入目标分支”。Pisper 的 Git 服务也主要面
  向当前工作区直接操作；其 revert 会执行 git reset --hard 和 git clean -fd，不适合替代 DevLoop 的隔离 Worktree
  审核模型。

  建议 DevLoop 借鉴的优先级

  1. P0：任务上下文层
     为任务增加可控的“执行上下文摘要/历史”，而不只是把全部历史日志塞回 Prompt。迭代任务应能携带：上轮目标、审核
     反馈、结果摘要、关键文件、验证结果和 Commit 基线。

  2. P1：Skill 按任务选择与作用域
     当前 DevLoop 会将所有启用 Skill 注入 Runner Prompt。可借鉴 PisPer 的项目级/全局级、信任状态和按需激活方
     式，避免上下文膨胀与无关规则干扰。

  3. P1：扩展非代码任务时引入“工作流模板”
     不建议把现有任务状态机替换成 PisPer 式 DAG；应在其上新增研究、内容、测试、发布等模板，并保留开发任务专有的
     Worktree、Commit、审核和写入链。

  4. P2：把浏览器自动化开放给 Agent
     DevLoop 已有服务端自动验证。可以额外提供受限浏览器工具让 Agent 探索页面，但最终验收仍应由服务端固定环境重
     跑，不能以 Agent 自报为准。

  5. 谨慎引入插件执行
     PisPer 的插件虽然运行在 Worker 中，但代码仍拥有当前系统用户的文件和网络权限。DevLoop 若加入 MCP/插件，应先
     定义项目白名单、权限声明、审计和任务级启用策略。
