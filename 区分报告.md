# DevLoop vs Pisper 区分报告

> 生成日期：2026-08-20
> 依据：直接阅读两侧代码（DevLoop `/apps` `/packages` `/schemas` `/scripts`，Pisper `/runtime` `/src` `/src-tauri` `/src-tui` `/packages` `/shared` `/crates`），不依赖 README 和方案文档。
> 结论遵循「代码为准」，README 中承诺但代码未实现的能力不计入。

---

## 0. 一句话定位

- **DevLoop**：面向代码交付的任务控制台。核心是「隔离 Worktree 执行 → 结果 Commit → 人工审核 → 冲突处理 → 写入目标分支 → 自动预览与 Playwright 验证」。产品形态是 Electron + Fastify 服务端 + React Web，数据层是 SQLite + Drizzle 迁移 + 幂等命令表。
- **Pisper**：本地优先的多能力 Agent 工作台。核心是「长期会话 + 分层记忆 + 工具/MCP/插件生态 + 派生子 Agent + 可视化工作流 DAG + 定时调度 + 多渠道通知」。产品形态是 Tauri（Rust）壳 + Node SEA sidecar + React Web + Rust TUI。

两者虽都可运行本地 Coding Agent，但目标完全不同：**DevLoop 是「代码变更交付流水线」，Pisper 是「通用 Agent 工作台」**。

---

## 1. 架构对照

| 维度 | DevLoop | Pisper |
| --- | --- | --- |
| **桌面形态** | Electron 43，`utilityProcess.fork` 启动 Fastify 服务端（`apps/desktop/src/main/index.ts:87-117`） | Tauri v2（Rust）壳 + Node SEA sidecar，`ratatui` TUI 双入口（`src-tauri/src/lib.rs:30-56`，`src-tui/src/main.rs`） |
| **服务端** | Fastify 5，端口固定 4317，Pino 日志（`apps/server/src/index.ts:13-92`） | Node 原生 http，端口 0 随机，desktop 用 token cookie 鉴权（`runtime/sidecar.mjs:71-82`） |
| **前端** | React 19 + @tanstack/react-router + Ant Design 6 + Zustand（单例）+ react-query | React 19 + react-router-dom v7 hash router + zustand + react-query + dockview + xyflow + xterm + streamdown + i18next（中英双语） |
| **鉴权** | 单实例 owner，`resolveIdentity` 硬编码 editor 角色（`apps/server/src/http.ts:30-37`）；`paired_devices` 表已建但未接入 HTTP | Desktop token cookie；workspace trust store 控制项目级资源加载（`runtime/services/workspace-trust-service.mjs`） |
| **数据层** | SQLite（better-sqlite3）+ Drizzle Kit 迁移链 0000–0010 + 单文件 schema.ts + 幂等命令表（`remote_commands`） | Node 内置 `node:sqlite`（仅记忆），核心状态用 JSON 原子文件（`runtime/storage/json-file.mjs`），provider/skills/plugins/mcp/schedules/workflows/goals/plans 都是 JSON |
| **实时通道** | 单一 SSE `GET /api/events`，支持 Last-Event-ID 和游标回放（`apps/server/src/app.ts:1015-1043`） | HTTP + 事件流（未直接看到 WebSocket），前端通过 react-query 轮询与流响应 |
| **多进程/并发** | 单 AgentWorker，`concurrency_limit` 1–10；每次任务在独立 git worktree 中执行 | 派生子 Agent（`spawn_agent`），全局并发上限 4、单父上限 64、总记录 256（`runtime/services/multi-agent-service.mjs:22-27`） |

---

## 2. 核心领域模型

### DevLoop：任务交付流水线

- **实体**：`Project → Task → TaskRevision → TaskRun → RunEvent + ReviewDecision`（`packages/shared/src/domain.ts`）
- **任务状态机**：`DRAFT / READY / RUNNING / REVIEW / BLOCKED / FAILED / COMPLETED / CANCELLED`，转换由 `assertTaskTransition` 强制校验，同时在 `packages/workflow/src/task-machine.ts` 有 XState 镜像（测试用）
- **Run 状态机**：`CLAIMED / PREPARING / AGENT_RUNNING / VERIFYING / REPAIRING / PREPARING_REVIEW / SUCCEEDED / BLOCKED / FAILED / INTERRUPTED / CANCELLED`（`agent-worker.ts:28-34` 用事件→阶段映射）
- **Revision 是不可变快照**：每次确认或驳回都写一条新的 `task_revisions`，冻结 `specJson` + `specHash` + `baseStrategy` + `confirmedBaseCommit`，驳回还会带 `continuationBaseCommit/continuationResultCommit`，让下一轮在上轮 result 提交上继续（`run-review-repository.ts:407-517`）
- **审核 = 交付闸门**：`approve` 分三条路——`remote`（推目标分支）、`local`（写入工作树）、`research`（无代码，仅归档）
- **幂等与乐观锁**：所有可变仓储调用走 `executeIdempotent(deviceId, idempotencyKey, commandType, expectedVersion, action)`，任意重放返回同一 JSON；`projects/tasks/paired_devices/skills/worker_state` 都有 `version` 列

### Pisper：会话 + 目标 + 计划 + 工作流

- **实体**：`Session（Pi 引擎 JSONL 持久化）→ Turn/Message → Memory → Goal → Plan → Workflow(Node graph) → Schedule → Run`
- **Session Tree**：Pi 引擎 JSONL 会被投影成派生分支树（`runtime/runtime/session-tree.mjs`），支持从历史节点派生子会话
- **Goal**：明确的 token 预算与状态 `active / paused / budget_limited / complete`，默认 30 000 tokens（`runtime/services/goal-service.mjs`）
- **Memory**：SQLite + FTS5 + 本地 384 维 FNV-1a 哈希嵌入向量，space 分 `global / project / custom`，type 分 `concept/file/risk/preference/decision/fact/task`，候选流 `propose → accept/reject`，置信 ≥0.6 自动接受（`runtime/services/memory/local-memory-runtime.mjs`）
- **Workflow**：完整 DAG 节点集 `trigger / prompt / skill / file / mcp / notification / condition / parallel / approval`，含条件分支、并行、审批、失败重试、超时；由 `shared/workflow-graph.mjs` 做循环与端口检测
- **Schedule**：`interval / daily / weekly / monthly` + 时区感知的下次运行计算（`runtime/services/schedule-service.mjs`）

---

## 3. 逐能力对照

### 3.1 Agent 执行

| 项 | DevLoop | Pisper |
| --- | --- | --- |
| **谁执行** | 外部 CLI：Codex CLI 或 Claude Code CLI，通过 `--print --output-format json` 一次性拿最终 AgentResult | 内置 `@earendil-works/pi-coding-agent`（Pi Coding Agent），Pisper 自己是 host |
| **上下文管理** | 一次性 Prompt：`buildTaskPrompt(title/goal/ACs/skills/schema)`；跨 Run 只透传 `RetryContext`（上一次失败事件日志的摘要） | 长会话 + 派生分支 + `compaction-policy.mjs`（默认 80% 上下文窗口触发压缩，保留最近 20 000 tokens） |
| **模型/Provider 管理** | 项目级 `runner` 字段（`codex` \| `claude-code`），CLI 自身管模型 | `ProviderPreferences` + `ProviderDiscoveryService`（读取本地 Codex/Claude 凭据）+ `ProviderModelCatalogService`；支持 openai-responses/completions/image/video、openrouter、google、xai、`new-api` |
| **思考等级** | 无（CLI 决定） | 支持 `off/minimal/low/medium/high/xhigh/max`，按模型家族映射（GPT_XHIGH, CLAUDE_FABLE, GLM_52, DEEPSEEK_V4 等） |
| **派生子 Agent** | 无 | `spawn_agent / list_agents / send_message / wait_agent / interrupt_agent / followup_task`，父子邮箱通信，父级最多 4 并发 |
| **中断/取消** | 旋转 `executionToken` 让 DB 层令牌失效 + `kill(-pgid, SIGTERM)` 终止进程组 | Abort force-timeout 10 s（`ABORT_FORCE_TIMEOUT_MS`），会话级 abort |

### 3.2 工具生态（Agent 能调用什么）

| 项 | DevLoop | Pisper |
| --- | --- | --- |
| **内置工具集** | 由外部 CLI 决定；Codex Runner 显式禁用 browser/hooks/plugins/multi_agent（`codex-runner.ts:40-53`） | `read/ls/grep/find/edit/write/bash`，risk 分级，预设 `read-only/workspace/full`；host bash 会剥离凭据环境变量并走 `command-guard.mjs` |
| **应用级工具** | 无（DevLoop 服务端提供的 preview/playwright 是「验证」，不作为 Agent 工具暴露） | `web_search / browser_automation / generate_visual / skill_create / plugin_create / memory / mcp / task_list / tool_discovery / tool_gateway / multi_agent / goal / plan`（`runtime/tools/app/`） |
| **MCP** | ❌ 完全没有。Codex/Claude CLI 也被参数关掉 MCP 通路 | `@modelcontextprotocol/sdk` v1.29，stdio/SSE/streamable HTTP 三种传输；限制：32 servers × 500 tools × 256 KB schema × 4 MB per server × 100 tools/call × 60 s 默认超时 |
| **插件运行时** | ❌ 无 | Node Worker 线程沙盒 + TypeBox 参数校验；限制：512 文件 / 20 MB / 256 KB 清单 / 1 MB 结果 / 2 min 超时（`runtime/services/tool-plugin-service.mjs`） |
| **浏览器自动化** | 只做「验证」：服务端在 result commit 上启预览 + Playwright chromium 截图（`playwright-validation-service.ts`），结果作为 Artifact 存证；**Agent 不能主动开浏览器** | `browser-automation-service.mjs` 直接把 playwright-core 暴露给 Agent 做 open/navigate/click/type/screenshot/extract-text |
| **视觉生成** | ❌ 无 | `visual-generate.mjs` 走 OpenAI 兼容图像/视频端点 |

### 3.3 Skill / Prompt 层

| 项 | DevLoop | Pisper |
| --- | --- | --- |
| **来源** | 只有一处：DB `skills` + `skill_versions` + 磁盘 `<data>/skills/versions/<uuid>/SKILL.md` | 三处叠加：Pi 引擎自带 skills（package）+ 用户级 `~/.agents/skills` + 项目级 `.pisper/skills`（trust 后加载） |
| **粒度** | 单文件 Markdown + YAML frontmatter（`name`, `description`）；`name` 匹配 kebab-case | 单文件 SKILL.md，另外含 `modelInvocation` 覆写；skill-service 会持久化 enabled/override 状态 |
| **注入方式** | 每次 Run 前把**所有 enabled** skills 全量拼进 Prompt（`buildSkillsPrompt`），总长上限 200 000 字符，超限直接抛异常；快照写入 `task_runs.skill_snapshot_json`（可审计） | 由 Pi 引擎内部按需激活，`modelInvocation` 决定是否显式加入上下文；不会一次全量塞 |
| **信任模型** | 无信任状态，任何 enabled 都用 | `WorkspaceTrustService` 门槛：`trusted / restricted / requiresDecision / inherited`，未信任项目不会加载 `.pisper/` 资源 |

### 3.4 Git / VCS

| 项 | DevLoop | Pisper |
| --- | --- | --- |
| **是否隔离** | ✅ 每个 Run 一个 `git worktree add -b <run-branch> <path> <baseCommit>`，`GitWorktreeService` 全权管理（`packages/git/src/git-worktree-service.ts`） | ❌ 直接在当前 workspace cwd 上操作 |
| **提交** | `commitWorktree` 用 `-c core.hooksPath=/dev/null --no-verify` 保证不触发钩子；每个 Run 产生一个 result commit | `GitChangesService.commit`：`git add -A && git commit -m …`，超时 30 s |
| **推送** | Approve → `moveWorktreeToCommit` 校验（`TARGET_BRANCH_CHANGED / WORKTREE_DIRTY / RESULT_COMMIT_MISSING`）后写入目标分支 | `push` 有 `--set-upstream` 回退，超时 120 s |
| **冲突处理** | `git-conflict-preview-service` + `POST /api/runs/:runId/resolve-conflicts`：spawn 一个 conflict-resolution 模式的 Runner 只让其编辑冲突路径 | 无原生冲突处理，冲突交给 Agent 自己在 workspace 里解决 |
| **回滚** | 用 executionToken + 幂等命令表回滚状态；不动用户工作树 | `revert` 走 `git reset --hard && git clean -fd`，直接影响当前工作树 |
| **VCS 兼容** | 仅 Git | Git + SVN 并行实现（`GitChangesService` / `SvnChangesService` / `VcsChangesService` 调度器） |

### 3.5 记忆 / 持久上下文

| 项 | DevLoop | Pisper |
| --- | --- | --- |
| **长期记忆** | ❌ 完全没有 | ✅ 完整方案：SQLite + FTS5 + 本地哈希嵌入向量（384 维 FNV-1a + 2/3-gram CJK） |
| **space 隔离** | — | `global / project / custom` |
| **memory type** | — | `concept / file / risk / preference / decision / fact / task` |
| **写入流程** | — | 候选 → 阈值 0.6 自动接受 → 30 天保留候选 / 90 天保留墓碑 |
| **密钥脱敏** | 只在 Codex JSON 流里做基础 API-key 掩码 | 全局 `secret-redaction.mjs`，memory 写入前统一过一遍 |
| **跨 Run 上下文** | 仅 `retryContext`（上一次失败 Run 事件日志的截断摘要，走 `parseTaskRevisionSpec`） | 会话本身持久，memory 主动检索（cosine + FTS + 关键词重叠 blend）|
| **Session 压缩** | — | `compaction-policy.mjs` 在 turn 边界自动 compact，阈值可调 50%–95%，默认 80% |

### 3.6 审核与批准

- **DevLoop 的审核**是**交付闸门**：审核结果决定 result commit 是否写入目标分支。审核信息（feedback）会以「继续基线」的形式反馈到下一个 revision（`continuationBaseCommit`），实现「上一次的产出作为下一轮起点」的迭代循环。
- **Pisper 的审批**是**工作流节点是否继续**：`approval` 是 workflow 的一种节点，超时 1 min – 7 天可配置。此外还有更细粒度的**工具审批**——`SessionPermissionService` 按 (cwd, tool, args) 生成审批 key（bash 只按命令文本），SHA-256 去重，5 min 缓存，256 条上限。

两者语义不同，不可互相替代。

### 3.7 预览 / 验证

- **DevLoop**：
  - 服务端 `PreviewService` 在 result commit 上创建**独立 detached worktree**，识别 pnpm-lock / package-lock / yarn.lock / bun.lockb 自动安装依赖
  - 端口零动态分配，`{{port}}` 占位符替换
  - 环境变量白名单：`PATH / HOME / PNPM_HOME / VITE_* / NEXT_PUBLIC_* / PUBLIC_*`
  - `PlaywrightValidationService` 启 chromium 抓 screenshot + 收集 console/page errors + 可选跑项目自定义 `playwrightTestCommand`
  - 结果作为 `playwright-report` + `playwright-screenshot` Artifact 归档，绑定到具体 Run
  - Electron 侧可开 sandbox BrowserWindow，session partition 隔离，`onBeforeRequest` 只放行预览 origin
- **Pisper**：无预览闸门，Agent 自己调用 `browser_automation` 工具去做交互式访问；截图是 Agent 主动产出，不作为验收凭证

### 3.8 工作流 / 编排

- **DevLoop**：只有任务状态机（DRAFT→READY→RUNNING→…），不是通用编排。`packages/workflow` 目录只有 25 行的 XState 镜像，主要给测试用。
- **Pisper**：完整可视化 DAG（xyflow/react）
  - 节点类型：`trigger / prompt / skill / file / mcp / notification / condition / parallel / approval`
  - 条件运算符：`exists / not_exists / equals / not_equals / contains / greater_than / less_than`
  - 失败策略：`stop / skip`；重试 0–3；每节点超时 1–240 分钟；每节点权限 `read-only / workspace-write / full-access`
  - 模板变量：`{{inputs.x}} / {{previous.output}} / {{nodes.<id>.output}}`
  - 图分析：cycle detection + port validation + unconnected trigger 检测（`shared/workflow-graph.mjs`）
- **调度**：DevLoop 无；Pisper 有 `interval / daily / weekly / monthly`、时区感知（Intl.DateTimeFormat + DST 迭代修正）、启动补偿（`RESTART_INTERRUPTED_ERROR`）

### 3.9 通知与外部集成

| 项 | DevLoop | Pisper |
| --- | --- | --- |
| **通知渠道** | 无（内部 SSE 用于 UI 同步） | `browser / feishu / weixin`（`runtime/services/channels/*` 双实现） |
| **消息接入** | 无 | Feishu WebSocket（@larksuiteoapi/node-sdk）+ WeChat 自研协议 |
| **模型元数据** | 无（依赖 CLI） | 拉 `https://models.dev/api.json` 缓存 |
| **Web 搜索** | 无 | Bing RSS（无需 API key），可配 safe-search + market |
| **Office 解析** | 无 | `officeparser` 惰性加载，附件为 office 文档时解析，上限 400 000 字符 |
| **桌宠** | 无 | `WebDesktopPetService` + Tauri window + `petdex.dev` 皮肤市场 |

### 3.10 权限与隔离

| 项 | DevLoop | Pisper |
| --- | --- | --- |
| **执行模式** | 隐含单模式，通过外部 CLI 参数控制 sandbox（Codex `--sandbox workspace-write`） | 会话级：`read-only / approval-required / workspace-write / full-access`（`runtime/security/execution-mode.mjs`）；`plugin_create` 只在 `full-access` 下可用 |
| **命令白名单/黑名单** | 无 | `runtime/tools/command-guard.mjs`（~19 KB）拦截危险命令 |
| **凭据剥离** | 只在 Codex JSON 输出做正则 | host bash 显式剥离 `ANTHROPIC_API_KEY / OPENAI_API_KEY / GITHUB_TOKEN` 等 + 正则 `API_KEY / TOKEN / SECRET / PASSWORD / PRIVATE_KEY` |
| **多设备/多角色** | `viewer / operator / editor` 枚举与 `paired_devices` 表已建，但 HTTP 层未接入；`requireLocalRole` 仅根据 loopback 限流 | Desktop token cookie 一对一；workspace trust 控制项目级资源 |
| **预览沙盒** | Electron sandboxed BrowserWindow + session partition + origin 白名单 | 无（Tauri WebView 是主窗口） |

---

## 4. DevLoop 明显更强的部分

1. **代码交付链完整且强约束**
   - Worktree 隔离 → Commit → Diff → 审核 → 冲突处理 → 目标分支写入
   - `executionToken` 让「杀死进程」和「作废结果」原子化
   - 幂等命令表使得任何 REST 调用都可以安全重放，多端/多次操作不冲突
2. **审核后可基线化迭代**
   - 驳回时保留 `continuationBaseCommit/continuationResultCommit`，下一轮 Revision 以「上轮产出」为起点，不是回到零重来
3. **预览/验证与结果 Commit 绑定**
   - Agent 自称的「测试过了」不作数——服务端在固定环境里跑，Playwright 屏幕截图 + console error + report 三件套写入 Artifact
   - 自动识别常见前端启动方式与包管理器
4. **迁移与审计基础扎实**
   - Drizzle Kit 0000–0010 迁移链 + 完整 snapshot；`domain_events` monotonic 序列供 SSE 回放；`Last-Event-ID`/`?after=` 游标可断线续订
   - 类型化 Zod 参数校验 + Repository 错误码到 HTTP 状态码的统一映射

---

## 5. Pisper 明显更强的部分

1. **通用自动化编排** — 可视化 DAG + 条件/并行/审批/通知/重试/超时 + 定时调度（含 DST），并非只服务代码任务
2. **模型层解耦** — 多 Provider、多模型、思考等级、Provider 自动发现、模型元数据缓存
3. **长期记忆** — SQLite + FTS5 + 本地嵌入向量 + 候选/接受/墓碑生命周期 + 密钥脱敏
4. **子 Agent 与消息** — `spawn_agent / send_message / wait_agent`，分治并行，父子邮箱
5. **工具生态** — MCP（stdio/SSE/streamable HTTP 三传输）、Node Worker 沙盒插件、浏览器自动化工具、web_search、visual generate
6. **执行模式细粒度** — read-only / approval-required / workspace-write / full-access + 命令 guard + 凭据剥离
7. **多渠道接入** — Feishu / WeChat / Browser 通知与消息双向
8. **多端形态** — Tauri Webview + Rust TUI + Node SEA sidecar + npm 平台分发 + 组件签名更新

---

## 6. 关键区别与陷阱

- **审批语义不同**：Pisper `approval` 是「工作流节点是否继续」，DevLoop 是「代码结果是否允许写入目标分支」——不可互换。
- **Git 操作破坏性不同**：Pisper `revert` 直接 `git reset --hard && git clean -fd` 当前工作树；DevLoop 依赖 worktree 隔离，用户工作树不受影响。
- **上下文承接方式不同**：Pisper 靠 session 持久化 + memory 检索；DevLoop 靠 revision 快照 + 结构化 `retryContext`，不带整段对话。
- **Skill 注入策略不同**：DevLoop 一次全量塞 Prompt，Pisper 按需激活。项目大时 DevLoop 会遭遇 200 KB 上限或 token 膨胀。
- **数据层选择不同**：Pisper 多数运行时状态是 JSON 原子文件（重启后即失易受损问题少但难查询），DevLoop 是 SQLite + 迁移（可审计强、可分析）。
- **前端形态不同**：DevLoop Ant Design + 中文单语，Pisper shadcn/radix + i18next 中英双语 + dockview 多面板。

---

## 7. DevLoop 需要补充的能力（按优先级）

> 说明：不建议把 DevLoop 改造成 Pisper。DevLoop 的「代码交付闸门」是核心资产。以下补充点应在保留 Worktree/Commit/审核 主干的前提下，把「Agent 端能力」和「非代码任务能力」补齐。

### P0 — 交付流水线本身的短板

1. **任务上下文层增强**
   现状：跨 Run 只有 `retryContext`（上一次失败事件日志），信息稀薄。
   建议：把「上轮目标、审核 feedback、结果摘要、关键文件、验证结果、Commit 基线」结构化成 `TaskContextDigest`，每次 Confirm 时写入 revision，Runner Prompt 里显式列出。类比 Pisper 的 memory 检索，但**不需要长期记忆**——只需要「本任务历史」结构化聚合。
   影响文件：`packages/db/src/schema.ts` 增加或复用 `task_revisions.contextDigestJson`；`packages/runners/src/task-prompt.ts` 增补渲染逻辑。

2. **Skill 按需激活与作用域**
   现状：所有 enabled skill 一次性塞 Prompt，200 KB 硬上限。
   建议：
   - 引入 skill scope：`global / project / task-type / task`
   - 引入 activation：`always / on-match(pattern) / manual`
   - `RegisteredProjectInput` 增加 `enabledSkillIds` 白名单
   - 参考 Pisper `SkillsService` 的 override 机制
   影响面：`SkillService.listEnabledForExecution` 需按 project + taskType 过滤；DB 需要 `project_skill_bindings` 或在 project row 内嵌 JSON。

3. **鉴权真正接入**
   现状：`resolveIdentity` 硬编码 owner+editor；`paired_devices/pairing_sessions/remote_commands` 表已建但 HTTP 未接入。
   建议：至少走通「配对码 → 生成 device token → HTTP 中间件校验 → 角色路由守卫」四步；`requireLocalRole` 之外补上 `requireDevice`。这是多端使用（尤其 desktop 之外的场景）前置条件。

### P1 — Agent 端能力扩展

4. **MCP 支持（谨慎版）**
   现状：Codex Runner 硬编码 disable MCP，Claude Code Runner 无 MCP 配置。
   建议：不在 Runner 命令行开 MCP（会破坏隔离），而是 **DevLoop 服务端自己变成 MCP host**，把「Playwright 验证」「预览启动」「读取 Artifact」「查询 Task History」暴露为 MCP tool，供当前 Run 期间的 Runner 使用。这样：
   - Agent 可以查任务历史（不只是 retryContext）
   - Agent 可以触发预览
   - 权限仍由服务端把关
   参考 Pisper 的 `mcp-service.mjs` 上限设置（32 servers × 500 tools × 60 s 超时）。

5. **非代码任务模板（不是替换状态机）**
   现状：`taskType` 只有 `DEVELOPMENT / RESEARCH`。
   建议：新增「测试」「内容」「发布检查」等模板，共享 Worktree+Commit+审核主干；参考 Pisper `workflow-service.mjs` 的节点类型，但作为「任务子类」而不是通用 DAG。

6. **浏览器工具开放给 Agent（只读 + 白名单）**
   现状：只有服务端做验证。
   建议：开一个仅面向 Runner 的「受限浏览器工具」，只允许访问 preview URL、只 screenshot/click/type/extract 五个动作，配合服务端最终 Playwright 验证兜底。参考 Pisper `browser-automation-service.mjs` 的接口，但**保留服务端固定环境重跑作为验收凭证**——不能让 Agent 自报为准。

### P2 — 通用/生态

7. **命令与凭据 Guard**
   现状：只有 Codex JSON 输出做 API-key 掩码。
   建议：借鉴 Pisper `command-guard.mjs` + host bash 环境变量剥离，加到 Worktree 内的执行入口——即使 Runner 沙盒失效也有第二道防线。

8. **告警/通知渠道**
   现状：Web 侧 SSE 用于 UI 实时刷新；审核 pending、Run 失败、冲突、Playwright 失败没有主动推送。
   建议：加一个渠道抽象（先只做 Browser Notification 和 Webhook），后续再考虑 Feishu/WeChat。参考 Pisper `channel-service.mjs` 但只保留通知方向，不引入消息回投。

9. **审计事件真正落库**
   现状：`audit_events` 表已建但读路径未见写入。
   建议：把 approve/reject/switch runner/skill enable/preview 启停都写入 `audit_events`，配合 `domain_events` 提供「审计视图」页面。

10. **i18n**
    现状：Ant Design zh_CN 直接引入，所有错误消息与 UI 拷贝都是中文硬编码。
    建议：至少把服务端 Zod error map 与 API 错误消息做键化（不必立刻做中英切换）；前端引入 i18next（参考 Pisper `src/locales/{zh-CN,en-US}` 与 `scripts/check-i18n.mjs`）。

### P3 — 可选借鉴，不推荐直接引入

- **可视化 DAG 编排**：Pisper 的 xyflow DAG 很酷，但引入会侵蚀 DevLoop「交付流水线」核心定位。建议至多做「任务链 / 前置依赖」而非通用 DAG。
- **长期记忆 + 向量检索**：DevLoop 的场景是短期任务+审核，长期记忆价值有限。若真要引入，也应绑定到 Project 而非 Global，避免跨项目泄漏。
- **插件运行时**：Pisper 用 Node Worker 沙盒，权限仍是当前用户级。DevLoop 引入前必须先定义项目白名单、权限声明、审计、任务级启用策略，且**不能**运行在服务端进程内——建议 Runner 侧独立进程沙盒或直接不做。
- **子 Agent（`spawn_agent`）**：与 DevLoop「一 Run 一 Worktree」的隔离模型不兼容；除非把子 Agent 也绑到独立 Worktree，否则不建议加入。
- **SVN 支持**：可当作后续 `packages/git` 的 sibling 实现，但当下需求不明显。

---

## 8. 汇总优先级速查表

| 优先级 | 能力 | 主要文件 | 备注 |
| --- | --- | --- | --- |
| P0 | TaskContextDigest（结构化上轮上下文） | `packages/db/src/schema.ts`、`packages/runners/src/task-prompt.ts` | 交付质量最直接提升 |
| P0 | Skill scope + on-demand activation | `apps/server/src/skill-service.ts`、`agent-worker.ts` | 解决 200 KB Prompt 天花板 |
| P0 | 鉴权接入 paired_devices / roles | `apps/server/src/http.ts`、新增 `apps/server/src/routes/auth.ts` | 多端使用前置 |
| P1 | 服务端做 MCP host | 新增 `apps/server/src/mcp/…` | Agent 与服务端能力互通 |
| P1 | 任务模板化（测试/内容/发布） | `packages/shared/src/domain.ts`、`agent-worker.ts` | 保住核心状态机 |
| P1 | 受限浏览器工具给 Agent | `packages/runners/src/…`、`apps/server/src/preview-service.ts` | 服务端 Playwright 仍兜底 |
| P2 | Command guard + 凭据剥离 | `packages/runners/src/…` | 防御纵深 |
| P2 | 通知渠道抽象 | 新增 `apps/server/src/notify/` | 至少浏览器通知 + webhook |
| P2 | audit_events 写入 + 审计视图 | 各 repository + 新增 audit route | 已有表结构，只差写入 |
| P2 | i18n | `apps/web`、`apps/server` | Web 首屏字符集 |
| P3 | DAG / 长期记忆 / 插件 / 子 Agent / SVN | — | 生态型，需求驱动再评估 |

---

## 附：证据索引

**DevLoop**
- 服务端启动：`apps/server/src/index.ts:13-92`
- HTTP 路由与角色：`apps/server/src/app.ts`（37 路由）、`apps/server/src/http.ts:21-59`
- 数据模型：`packages/db/src/schema.ts:22-285`
- 仓储与幂等：`packages/db/src/repositories/*`（`repository-base.ts:33-74`, `run-claim-repository.ts:22-151`, `run-review-repository.ts:31-517`）
- Runner 抽象：`packages/runners/src/types.ts:54-58`、`codex-runner.ts:27-541`、`claude-code-runner.ts`
- AgentWorker：`apps/server/src/agent-worker.ts:121-522`
- Worktree/冲突：`packages/git/src/git-worktree-service.ts:19-133`、`git-conflict-preview-service.ts`
- 预览/验证：`apps/server/src/preview-service.ts:48-121`、`playwright-validation-service.ts`
- 桌面：`apps/desktop/src/main/index.ts:40-465`、`apps/desktop/src/preload/index.cts`
- 迁移链：`packages/db/drizzle/0000_… 0010_task_type.sql` + `meta/_journal.json`

**Pisper**
- Runtime 入口：`runtime/index.mjs:10-52`、`runtime/sidecar.mjs:25-107`、`runtime/app-runtime.mjs:24-156`
- Pi Coding Agent 包装：`runtime/runtime/pi-coding-agent.mjs:5-124`
- 会话与派生：`runtime/runtime/agent-runtime.mjs:1-155`、`session-tree.mjs`
- 多 Agent：`runtime/services/multi-agent-service.mjs:22-64`
- 记忆：`runtime/services/memory/local-memory-runtime.mjs:7-43`、`local-embedding.mjs`
- 压缩：`runtime/runtime/compaction-policy.mjs:5-80`
- Workflow / Schedule：`runtime/services/workflow-service.mjs:12-286`、`schedule-service.mjs:9-56`、`shared/workflow-graph.mjs`
- MCP / 插件 / Skill：`runtime/services/mcp-service.mjs:17-45`、`tool-plugin-service.mjs:21-47`、`skills-service.mjs:127-158`、`workspace-trust-service.mjs:12-91`
- 工具目录：`runtime/tools/builtin-catalog.mjs:3-122`、`host-bash.mjs:13-53`、`command-guard.mjs`、`app/index.mjs:1-45`
- Git/SVN：`runtime/services/git-changes-service.mjs:7-157`、`svn-changes-service.mjs`、`vcs-changes-service.mjs`
- 桌面：`src-tauri/src/lib.rs:30-56`、`src-tui/src/main.rs:61-1214`、`scripts/build-sea.mjs`、`packages/pisper/bin/pisper.mjs:9-24`
- 提供者/模型：`runtime/services/provider-discovery.mjs`、`provider-model-catalog-service.mjs`、`model-metadata-service.mjs:1-6`
- 渠道：`runtime/services/channels/{channel-service,feishu-gateway,weixin-gateway}.mjs`
