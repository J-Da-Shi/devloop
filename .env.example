# 宿主机只暴露给本机反向代理的端口
DEVLOOP_PORT=4317

# 默认使用真实 Codex；首次部署可临时改为 fake 验证页面
DEVLOOP_RUNNER=codex
DEVLOOP_CODEX_IGNORE_USER_CONFIG=false
DEVLOOP_CODEX_TIMEOUT_MS=1800000
DEVLOOP_AGENT_CLAIM_DELAY_MS=5000
DEVLOOP_LOG_LEVEL=info

# Codex CLI 镜像版本
CODEX_CLI_VERSION=0.145.0

# 宿主机上的挂载目录
DEVLOOP_CODEX_DIR=./config/codex
DEVLOOP_SSH_DIR=./config/ssh

# 使用 API Key 时填写；已在挂载的 Codex 配置中登录则可以留空
OPENAI_API_KEY=
CODEX_API_KEY=

# 自定义 Provider 或代理按需填写
HTTP_PROXY=
HTTPS_PROXY=
NO_PROXY=127.0.0.1,localhost,devloop
