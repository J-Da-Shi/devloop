# DevLoop Agent 工作规范

本文件适用于在此仓库中执行开发、修复、重构和审核任务的所有 Agent。

## 工作原则

- 先阅读与任务直接相关的源码、类型和测试，再决定修改方式；不要依据方案文档替代代码事实。
- 遵循现有架构、命名和依赖约定，优先复用已有模块与能力，避免无关重构。
- 保留用户已有的未提交改动；不要擅自还原、删除或覆盖与当前任务无关的内容。
- 只实现任务所需的最小完整改动，并在完成后运行与改动范围相称的检查或测试。
- 不要修改 `AGENTS.md`，除非任务明确要求更新项目规范。

## 开发规范

- 每个手写源码文件不得超过 700 行代码。新增或修改后可能超出限制时，必须按清晰职责拆分为多个模块；已超限的文件被修改时，也应在本次改动中拆分到限制以内。
- 文件名必须准确反映其内容和职责，使用项目既有的命名风格。禁止使用含义模糊的名称，例如 `utils`、`helpers`、`common`、`misc`、`new`；通用能力也应以具体领域或用途命名。
- 一个模块只承担内聚职责。页面、业务流程、数据访问、运行器适配、类型定义和可复用 UI 应按职责拆分，避免把无关逻辑堆入同一文件。
- 为新增或变更的行为补充或更新测试；修复缺陷时优先添加能够复现该问题的测试。
- 不引入未使用的依赖、导出、代码或配置。除非任务需要，不修改生成文件、锁文件和构建产物。
- 对外输入、文件路径、命令参数和持久化数据必须进行适当校验；不得把密钥、令牌、个人路径或其他敏感信息写入源码、日志或测试夹具。

## 提交前检查

- 确认类型检查、相关测试和格式检查通过；若受环境限制无法执行，明确说明原因和未验证范围。
- 检查改动不会破坏现有 API、数据库迁移、任务状态流转或桌面端与服务端之间的契约。
- 在结果中简要说明改动内容、验证方式和仍然存在的风险。
